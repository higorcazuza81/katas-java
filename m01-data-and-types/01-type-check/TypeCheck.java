void main() {
    int days = 30;
    IO.println(days);

    // int balance;
    // IO.println(balance);
    // error: variable balance might not have been initialized

    // boolean approved = "true";
    // error: incompatible types: String cannot be converted to boolean

    double rate = 2;
    IO.println(rate);

    // int total = 10.5;
    // error: incompatible types: possible lossy conversion from double to int
}